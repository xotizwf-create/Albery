-- Клиент, пришедший в бота сам, — отдельный источник рабочего окна.
--
-- Переписка бизнес-аккаунта менеджера и переписка бота живут в одних таблицах: команда
-- видит их в одном окне, сделка заводится одинаково. Но канал доставки разный — у бота
-- нет бизнес-подключения, и ответ уходит обычным сообщением. Различает их source_key,
-- поэтому у нового канала должна быть своя строка-справочник (на неё ссылаются диалоги).

INSERT INTO funnel_workspace_sources (source_key, source_type, display_name)
VALUES ('telegram_bot', 'telegram_bot', 'Telegram-бот')
ON CONFLICT (source_key) DO NOTHING;
