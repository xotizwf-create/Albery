Flask==3.1.3
python-dotenv==1.2.2
requests==2.33.0
psycopg[binary,pool]==3.2.3
reportlab==4.5.1
# document reading for the Bitrix bot (pdf/docx/xlsx) — added 2026-06-18
pypdf>=4.0
python-docx>=1.1
# точечная правка PDF на месте и разбор его вёрстки (pdfedit/docconvert) — added 2026-07-27
pymupdf>=1.24
openpyxl>=3.1
defusedxml>=0.7.1
PyYAML>=6.0
# MTProto-сессия аккаунта @AlberyAIManager (tg_userbot): закрытые каналы и закрытые групповые
# чаты Bot API не отдаёт вообще. Стояло на проде вручную с 27.07.2026 — закреплено 2026-08-05,
# иначе чистая установка по этому файлу молча теряет доступ к закрытым чатам.
Telethon>=1.44
# Боевой WSGI-сервер для ролей web и mcp (07.08.2026). До этого прод крутился на встроенном
# сервере Flask (app.run), который сами его авторы называют учебным и запрещают в бою.
# Роль бота пока остаётся на run_5002.py — у неё свой слив живых ходов при остановке.
gunicorn>=22.0
