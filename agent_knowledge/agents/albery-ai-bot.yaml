slug: albery-ai-bot
instructions:
- Формат ответа / Оформление сообщений клиенту в Telegram
- Работа с клиентами / Общение в переписке
skills: []
# Клиентский LLM не вызывает MCP-инструменты напрямую: у connector нет
# per-dialog resource context, поэтому даже «безопасное» имя инструмента позволило бы
# prompt injection указать чужой deal_id/telegram_id. Данные сделки, условия и отправку
# выполняет только детерминированный orchestration-код с фактическими id текущего события.
tools: []
