[pytest]
pythonpath = .
testpaths = tests
addopts = --import-mode=importlib -ra
markers =
    db: test requires a live PostgreSQL (set DATABASE_URL); skipped otherwise
    integration: orchestration test using mocked external HTTP + fake DB
